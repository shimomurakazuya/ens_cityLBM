# Fujitsu A64FX on Flow (Nagoya Univ, Japan)
COMPILER = mpiFCCpx
CXXFLAGS = -O3 -std=c++14 -Kfast,parallel,openmp -Nlst=t
CXXFLAGS += -D NO_IOTHREAD
LDFLAGS = -Kfast,parallel,openmp
TARGET = $(OUT_DIR)/citylbm.flow_a64fx_omp

# Linker
LINKER = $(COMPILER)
